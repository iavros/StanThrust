# Documentation

This folder keeps project documentation that is meant to be read or regenerated outside the application runtime.

- `overleaf_solver_report/`: LaTeX source, generated CSV assets, and the checked solver report PDF.
- `overleaf_solver_report_bundle.zip`: upload-ready archive of the solver report package.

